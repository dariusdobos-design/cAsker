import type { ViewStyle } from "react-native";

/** Jednotný tieň pre všetky tlačidlá v mobilnej appke. */
export const buttonShadow: ViewStyle = {
  shadowColor: "#000",
  shadowOffset: { width: 0, height: 4 },
  shadowOpacity: 0.28,
  shadowRadius: 8,
  elevation: 8,
};
